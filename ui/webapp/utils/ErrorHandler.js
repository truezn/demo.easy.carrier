'use strict';
sap.ui.define(['sap/m/MessageBox'], function (MessageBox) {
  var oErrorHandler = {
    handleODataBatchReqFailed: function (oEvent) {
      // This is intentional
    },
  };

  return oErrorHandler;
});
